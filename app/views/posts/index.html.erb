<% if user_signed_in? %>
  Logged in with <%= current_user.email %>. <%= link_to "Log out", destroy_user_session_path %>
<% else %>
  <%= link_to "Log in", user_session_path %>
<% end %>

<% @posts.each do |post| %>
    <div id="picture-<%= post.id %>%">
    <%= image_tag(url_for(post.picture), width: 600) %><br />
    <%= post.caption %>
    </div>
<% end %>
<br />
<%= link_to "New", new_post_path %>